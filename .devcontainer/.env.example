# =============================================================================
# Configuration Claude DevContainer (claude-code-proxy Go)
# =============================================================================
# Copiez ce fichier vers .env et remplissez vos valeurs
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Provider: Mammouth.ai (OpenAI-compatible)
# -----------------------------------------------------------------------------
OPENAI_BASE_URL=https://api.mammouth.ai/v1
OPENAI_API_KEY=sk-votre-clé-mammouth-ici

# -----------------------------------------------------------------------------
# Model Routing: Claude → Mistral/Codestral
# -----------------------------------------------------------------------------
# Ces variables définissent le mapping des modèles Claude vers vos modèles

# Opus tier (modèle le plus puissant)
ANTHROPIC_DEFAULT_OPUS_MODEL=mistral-large-3

# Sonnet tier (modèle principal)
ANTHROPIC_DEFAULT_SONNET_MODEL=codestral-2508

# Haiku tier (modèle rapide/économique)
ANTHROPIC_DEFAULT_HAIKU_MODEL=mistral-small-3.2-24b-instruct

# -----------------------------------------------------------------------------
# Server Settings
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8082

# -----------------------------------------------------------------------------
# Configuration Air-Gapped (future)
# -----------------------------------------------------------------------------
# Pour un déploiement local avec Codestral:
# OPENAI_BASE_URL=http://codestral-server:8080/v1
# OPENAI_API_KEY=local-key
